#include <stdio.h>
#include <stdlib.h>

/**
 * _abs - Computes the absolute value of an integer
 *
 * @n: The input value
 *
 * Return: 0 (Successful)
 */

int _abs(int n)
{
	int absValue = abs(n);

	return (absValue);
}
